{%- if prod.handle != blank -%}
  <add-favorite
    class="cursor-pointer flex group {{ class }}"
    data-product-handle="{{ prod.handle }}"
    {% if var.id != blank %}
      data-variant-id="{{ var.id }}"
    {% endif %}
  >
    {%- render 'icon-heart',
      class: 'fill-transparent stroke-black transition-[fill] duration-300 ease-in-out 
      group-[[added]]:fill-black group-[[added]]:stroke-black'
    -%}
  </add-favorite>
{%- endif -%}
