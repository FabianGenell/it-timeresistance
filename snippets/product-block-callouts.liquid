{% capture content %}
  <div
    class="product__callouts-items"
    data-with-box="{{ block_settings.add_box }}"
    data-with-dividers="{{ block_settings.add_dividers }}"
  >
    {%- for i in (1..6) -%}
      {%- liquid
          
        assign text_setting = 'text_' | append: i 
        assign icon_setting = 'icon_' | append: i 
        assign custom_icon_image_setting = 'custom_icon_image_' | append: i 
      -%}

      {%
        render 'product-block-callouts-item' with
        text: block_settings[text_setting],
        icon: block_settings[icon_setting],
        custom_icon_image: block_settings[custom_icon_image_setting]
      %}
    {%- endfor -%}

  </div>
{% endcapture %}

{% if block_settings.display_as_collapsible_row %}
  {% assign heading = block.settings.title | default: block.settings.collapsible_row_title %}
  {%- render 'product-block-accordion',
    block: block,
    block_settings: block.settings,
    heading: heading,
    content: content
  -%}
{% else %}
  <div
    class="product__block product__block-callouts product__callouts product__block--medium"
    {{ block.shopify_attributes }}
  >
    {%- if block.settings.title != blank -%}
      <{{ block.settings.title_tag }} class="ff-heading fs-heading-5-base">
        {{- block.settings.title -}}
      </{{ block.settings.title_tag }}>
    {%- endif -%}
    {{ content }}
  </div>
{% endif %}
