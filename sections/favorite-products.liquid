<!-- File: sections/favorite-products.liquid -->
{%- if section.settings.enable_section_style -%}
  {%- render 'section-style', section: section -%}
{%- endif -%}
<favorite-products
  style="min-height: 100vh"
  class="block h-full relative"
  data-layout="grid"
  data-hide-on-removal="{{ section.settings.hide_on_removal }}"
>
  <h1 class="text-2xl block w-full mb-6 pb-2 border-b border-gray-200">
    {{ 'sections.favorite_products.title' | t }}
  </h1>

  <div
    data-product-grid
    class="grid grid-cols-2 md:grid-cols-4 gap-6"
  ></div>
  <div data-spinner class="flex justify-center my-8">
    <div
      class="loading-spinner w-12 h-12 rounded-full border-4 border-stone-300 border-t-stone-500 animate-spin"
      role="status"
      aria-label="{{ 'sections.favorite_products.loading' | t }}"
    ></div>
  </div>
  <div
    data-empty-state
    class="hidden mt-14"
  >
    <h3 class="text-center fs-heading-3-base ff-heading">
      {{ 'sections.favorite_products.empty_state_title' | t }}
    </h3>
    <p class="text-center">{{ 'sections.favorite_products.empty_state_description' | t }}</p>
  </div>
</favorite-products>

{% schema %}
{
  "name": "Favorite products",
  "class": "section",
  "tag": "section",
  "settings": [
    {
      "type": "checkbox",
      "id": "hide_on_removal",
      "label": "Hide on removal",
      "default": true
    }
  ],
  "presets": [
    {
      "name": "Favorite products"
    }
  ]
}
{% endschema %}
